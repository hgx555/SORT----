# 常见排序算法的函数库
包括：
简单插入排序
折半插入排序
希尔排序
快速排序
堆排序
归并排序（包含递归版与非递归版）

## 编译与执行方法
powershell中执行：
gcc test.c sort.c -o test.exe
.\test.exe
（注意test.c是你自己的需要调用函数的文件）

# 其它

## 注意堆排序函数的使用：
第一个有效元素必须从索引1开始，共有n个元素，结束索引应该是n

## 支持C plus plus